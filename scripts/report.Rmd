---
title: "Trajectory differential expression and cats"
output: html_document
---

# Trajectory differential expression {.tabset}

## Datasets
```{r setup, echo=FALSE, include=FALSE}
library(tidyverse)
knitr::opts_knit$set(root.dir = knit_root_dir)
```

```{r}
datasets <- inputs[["expression"]] %>% 
  map(read.table, row.names = 1, header = TRUE, sep = ",") %>% 
  map_df(function(expression) {
    tibble(
      n_cells = nrow(expression),
      n_features = ncol(expression)
    )
  })
```

```{r}
datasets %>% 
  gather("feature", "n") %>% 
  ggplot(aes(feature, n)) +
   geom_boxplot() +
  theme_bw()
```

## Evaluation

```{r}
scores <- 
  tibble(
    accuracy = map_dbl(inputs[["accuracy"]], jsonlite::read_json, simplifyVector = TRUE)
)

scores %>% 
  gather("metric", "value") %>% 
  ggplot(aes(metric, value)) +
    geom_boxplot() +
    theme_bw()
```

## Cats 

![](https://images.pexels.com/photos/104827/cat-pet-animal-domestic-104827.jpeg?auto=compress&cs=tinysrgb&h=350)
